{
  "name": "mrf-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start --workspace @mrf/web",
    "build": "npm run build --workspace @mrf/web",
    "test": "npm run test --workspace @mrf/web"
  }
}
